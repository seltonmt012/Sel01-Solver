# nexusluarelease.lua (5,667 lines, decompiled) — "nexus" release, icon `atom`

Libs `neverlose/events`, `clipboard`, `smoothy`, `base64`, `pui`. Three pui tabs: `house` Profile (About / Configs), `shield-cat` Anti Aim (General / Builder), `square-sliders` Settings (Features / Visuals / Other). Labels built as `"\f<icon>\r" + spaces + text`. Sidebar gradient wave. Configs in `db["NEXUS::USERDATA"]` (`nexus::cfg`): list `##PRESET_LIST`, input `##PRESET_NAME` def "Default", Load / Save / trash / copy (export) / paste (import).

## Anti-aim

**States**: `Default, Standing, Running, Slowwalking, Ducking, Sneaking, In Air, In Air & Crouching` (icons user / person / person-running / person-walking / person-seat / wheelchair-move / person-ski-jumping / person-sledding). `get_statement()`: `ducking = flags & 2`, `in_air = in_jump or not (flags & 1)`, `slowwalk = in_speed or NL Slow Walk`; air → 8 / 7; ducking or fake duck → vel > 2 ? Sneaking : Ducking; vel > 2 → slowwalk ? Slowwalking : Running; else Standing. Per state `Override <state>` switch (Default forced on + disabled); a state without Override uses Default.
**Per state**: `Yaw` Default (`Offset` -180..180) / `180° L/R` (`Left` / `Right` -180..180, `Randomize` 0-100 % (0 Off), `Delay` 1-20 t (1 Off), `Variability` 0-100 %); `Modifier` Disabled / Center / Offset / Random / Spin / X-Way (+ `Offset`, `Ways` 3-9); `Body Yaw` switch (`Left` / `Right` 0-60 def 0, `Freestanding` Off / Peek Fake / Peek Real, `Options` switches Avoid Overlap / Jitter / Randomize Jitter / Anti Bruteforce — passed straight to NL's option list); `Defensive > Enable` (`Pitch` Disabled / Down / Up / Random / Jitter / Custom with First / Second -89..89 or Offset; `Yaw` Disabled / Forward / Sideways / Random / Spin (Speed 1-30 def 3) / Center / Custom (Offset)).
**General**: Manual Yaw Disabled / Forward / Backward / Left / Right + Yaw Base Local View / At Target; Freestanding; Edge Yaw + While Fake Duck; Avoid Backstab; Safe Head + Weapon {Knife, Zeus} + Height Difference 0-35 def 20 (0 N/A, 35 Max); Jitter Move (`cmd.jitter_move`, def true). **Other**: Defensive `Compatible with Hide Shots` + `##CONDITIONS` listable (7 states); Disablers Warmup / No Enemy (→ `Angles > Enabled :override(false)`); Static Yaw {Freestanding, Manual Yaw}; Animation Breaker (Interpolation 0-14 (0 Default / 9 Medium / 14 High), Leaning 0-100, Ground None / Follow direction / Jitter legs / Moon walk / Kangaroo, Air None / Falling / Walking / Kangaroo); FL Disablers {Double Tap, Hide Shots, Standing}.
**Write point** (one createmove): `sended++` on un-choked; cfg = state with Override or Default; `even = sended % 2 == 0`; apply desync → yaw → modifier → static yaw (edge / manual / freestanding) → defensive LC → defensive angles → FL disablers → safe head; then `Enabled` (false when disablers active), `Yaw`, `Base`, `Offset`, `Avoid Backstab`, `Hidden`, `Yaw Modifier` + `Offset`, `Body Yaw` / `Inverter` / `Left` / `Right` / `Options` (table) / `Freestanding`, `cmd.jitter_move`, `Freestanding` (false with manual + static yaw). Runtime defaults `yaw "Disabled"`, base Local View, modifier Disabled, freestanding Off. Uses `rage.antiaim:inverter(bool)`, `get_target(true)`, `override_hidden_pitch / yaw_offset`, `rage.exploit:get()`, `allow_defensive(false)` for grenades. **No `cmd.force_defensive`.**
**L/R delayed switch**: `if sended >= next_switch then switch_side = not switch_side; next_switch = sended + max(1, randomize(variability, delay)) end; offset = switch_side ? randomize(rand, left) : randomize(rand, right)`; with Jitter in options `rage.antiaim:inverter(switch_side)`; otherwise offset follows `rage.antiaim:inverter()`. `randomize(pct, v) = v + (random()*2-1) * |v| * pct/100`.
**X-Way**: `i = sended % ways; half = floor(ways*0.5); if half <= i then (even ways → i+1); i == half → 0 end; return floor(offset * ((i-half)/half))`, applied as `yaw.offset = normalize_yaw(yaw.offset + that*0.5)`.
**Defensive detection**: negative sim-time delta: `d = to_ticks(sim.current - sim.old); if d < 0 then defensive_end = tickcount + |d| - to_ticks(latency) end; is_defensive = defensive_end > tickcount`. **Application** (only with DT on or HS + Break LC): body true, inverter false, limits 0/0, options `{}`, freestanding Off; defensive yaw ≠ Disabled → offset 0 + modifier disabled; pitch Custom / Random(First, Second) / Jitter (`even ? Second : First`) / `{Up -89, Down 89, Disabled 89}` → `override_hidden_pitch`; yaw Forward 180 / Custom / Sideways (`even ? 90 : -90`) / Random / Center (`even ? -amt*0.5 : amt*0.5`) / Spin (`spin_yaw = (spin_yaw + 180 * speed/10) % 360`) → `override_hidden_yaw_offset`. **LC gating**: state in conditions → HS off → `DT > Lag Options "Always On"`; Compatible + HS on → `HS > Options "Break LC"`; `Yaw > Hidden` = either and defensive enabled.
**Safe Head**: knife (classid 107) / zeus (268) per the listable, state string contains "In Air", `eye.z - threat_eye.z >= Height Difference` (skipped at 0) → yaw Backward, base At Target, offset -18, hidden false, modifier Disabled, body true, inverter true, limits 60/60, options `{}`, freestanding Off.
**Edge Yaw**: 12 rays (`for i = 30, 360, 30`) 1000u from the eye, shortest → `yaw "Static"`, offset = that angle, body false, modifier Disabled; short-circuits manual / freestanding.
**Manual**: offsets {Left -90, Backward 0, Forward 180, Right 90}; Static Yaw with Manual → hard `yaw "Backward"`, offset, body true, limits 60/60, modifier off; else ADDS the manual offset on top of the builder yaw. **Freestanding**: enabled (or via Peek Assist proxy) + `get_target(true)` + Static Yaw has Freestanding → the same hard set with offset 0. **FL disabler**: `Fake Lag > Enabled :override(false)` when DT / HS / standing per the selectable.
No built-in presets.

## Ragebot helpers (Settings > Features)

Peek Assist proxy + `Behaviors` {Quick Peek, Edge Stop, Extended Backtrack, Freestanding} (overrides `Peek Assist`, `Enabled > Extended Backtrack`, own Edge Stop / Freestanding); Dormant aimbot (Hitboxes Head / Chest / Stomach, Accuracy 35-100 def 75, Min. Damage 1-126 def 10 (1 Inherited, > 100 "HP+ N"), Auto scope; hitbox table `{Head scale 3 vec(0,0,58) ...}` corrected by duck amount (head -10*duck, chest -4*duck), `utils.trace_bullet` requiring `not target:is_visible()`, `cmd.view_angles` + `in_attack` when `get_inaccuracy() < 0.01`, `in_attack2` auto-scope on `weapon_type 5`, `cmd.block_movement = 1`); Logs (def on; Accent color {Miss, Hit}; Display Screen / Events / Console; hitgroup table `{[0]=generic, head, chest, stomach, left arm, right arm, left leg, right leg, neck, generic, gear}`); Decrease hold aim ticks (`sv_maxusrcmdprocessticks_holdaim 0`, restored 1).
Edge Stop: `sim = lp:simulate_movement(); sim:think(4); if sim.velocity.z ~= 0 then cmd.block_movement = 2 end`.

## Visuals

Custom Scope (color 9CD1FF "Inverts color", Offset 10-100 def 10, Length 10-100 def 50; `Scope Overlay "Remove All"`), Aspect Ratio (slider 0-300 + buttons 16:9 177.78 / 16:10 160 / 3:2 150 / 4:3 133.33 / 5:4 125; `r_aspectratio` on `net_update_end`), Viewmodel (FOV 0-1000 from `viewmodel_fov*10`, X / Y / Z -100..100 step .1, Opposite Knife Hand, Reset → fov 600 x 10 y 10 z 15), `$500` skeet-style Indicators listable {Safe Points, Double Tap, Hide Shots, Fake Duck, Body Aim, Minimum Damage, Dormant Aimbot, Freestanding, Fake Latency, Bomb Info, Hit Rate}, Keep Model Transparency (`localplayer_transparency` returns 59 when scoped and Chams Model Transparency has "In scope"), Screen Indicator + Glow, Manual Arrows Classic (Verdana 20 `<` `>`) / Modern (Verdana 27 `⮜` `⮞` + `render.shadow`) at ±45px, y shift -20 when scoped, Damage Indicator (Animated def true, Small; min damage `< 1 → "A"`, `> 100 → "+N"`, draggable), Watermark Fields {User, Time, Ping} + Custom Name, Keybinds, Spectators, Velocity Warning — all draggable windows (`slot_0_38_0.new(name):set_pos`) with a blur + glow background helper. Style: Accent (default `Link Active` alpha 153 → `pui.accent`), Glow (true), Blur (true).
**Crosshair stack** (draggable, `screen.y/2 + 15`): animated gradient `nexus` (+ `render.shadow`), `* STANDING *` (standing / moving / slowwalk / ducking / sneaking / in air / in air&c), then DUCK, DT (charge ring `render.circle_outline`), HS, BODY, SAFE, DMG, FS with smoothy alphas. **Skeet side list** (x=0, first entry `screen.y - 350`, double gradient bar + shadowed text, bomb SVG, right-hand percent ring): PING (white → 151,175,54 by fake-latency saturation), DUCK / DT / OSAA (mutually exclusive), BA, SP, MD, FS, bomb site + `-N HP` / `FATAL`, 20px defuse bar at the left edge, `Hit Rate %`, DA. Watermark / Keybinds (120px wide, 18px rows at `screen.x-140, 80`) / Spectators (`screen.x-140, 200`) / Velocity (center +100y), fonts `Trebuc 13 "ad"` / `Trebuc 20 "ad"`.

## Misc

Grenade features Throw fix / Super toss / Auto release (Damage 1-99 def 30, Molotov); No fall damage (`get_vfunc(76/77)` mins / maxs); Fast ladder; Game focus (Flash window def true, Auto focus; user32 + HWND opcode scan `8B 0D ?? ?? ?? ?? 85 C9 74 16 8B 01 8B` +8); Fake duck {Unlock speed (double forward / side on ground), Freeze time (1..14 counter → `in_duck = c > 7`, `send_packet = c == 14`, camera z +64)}; Air collision; Unlock latency; Edge Stop; Shared icon (voice_message protocol: `write_bits(ID, 32)`, `w:crypt("*v5_#fX9!zL2@mK8*")`, XOR_KEY 1514880045, ID 1064967572 / DEV 1064967573, icons from `raw.githubusercontent.com/emptyspotify/icons/main/nexus.png`). Animation breaker: `CAnimationLayer**` at `uintptr_t(lp[0]) + 10640` — leaning weight, pose params, interpolation via `tickinterval * ticks`. QueryPerformanceCounter load-time benchmark.
Events used: render, createmove, createmove_run, run_command, setup_command, net_update_start / end, post_update_clientside_animation, override_view, aim_ack, player_death, round_start, grenade_prediction, grenade_override_view, level_init / shutdown, localplayer_transparency, voice_message, mouse_input, shutdown.

## Extra ui.find paths

`Aimbot,Ragebot,Main,Enabled,Extended Backtrack`; `Aimbot,Anti Aim,Fake Lag,Variability`; `Visuals,Players,Self,Chams,Model` (+ `,Transparency`); `Miscellaneous,Main,Other,Windows`; `Miscellaneous,Main,In-Game,Clan Tag / Shared Features`; `Miscellaneous,Main,Movement,Air Strafe,WASD Strafe / Edge Jump`.
