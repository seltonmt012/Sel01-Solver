# spectraldev.lua (7,303 lines, decompiled) — "Spectral Dev", fork of the Everlast lua

Identity `var3.name = "Spectral"`, build Dev, date 01.01.25; preset DB key **`everlast-presets`**, db `spectral-db`. Libs `neverlose/base64`, `smoothy`, `inspect`. Three tabs: Home (`house`: labels, time, themes, Discord, config manager), Anti-Aim (`shield`: MAIN / ##OTHER col 1, BUILDER / ##BUILDER_LAYOUT col 2), Features (`sparkles`: list Ragebot / Visuals / Miscellaneous switching groups). Sidebar = per-frame color wave. Config elements are registered as `category::key` with collision detection.

## Anti-aim

**States** (three identical lists for Force Defensive / Defensive / Builder): `Standing, Moving, Slow Walk, Air, Air Crouch, Crouch, Crouch Move`. Resolver (createmove): on ground → NL Slow Walk ref → Slow Walk; not moving → Crouch / Standing; moving → Crouch Move / Moving; airborne → Air Crouch / Air. `is_moving = vel2d > 3.63`, `is_crouched = duck > 0`, ground = flags AND'd across `createmove` + `createmove_run`; `sent_packets++` only when `choked_commands == 0`; `abs_body_yaw = clamp(normalize_yaw(eye_yaw - abs_yaw), ±get_max_desync())`.

**Builder per state** (elements built from NL's own `:list()` option lists): `Enable <State>`; `Pitch` combo (NL list); `Yaw` combo (NL); `Base` (NL); `Left Offset` / `Right Offset` -180..180; `Yaw Modifier` (NL list) + `Mode` Default / Random (Min / Max Offset) / Custom (`Sliders` 3-6, `- 1..6`, random pick per tick); `Body Yaw` switch + `Mode` Default / Ticks (3-16: `body_yaw = false` when `tickcount % ticks <= 1`) / Random (4-16, same with a random period); `Left Limit` / `Right Limit` 0-60 def 60; `Options` (NL multi-select); `Freestanding` (NL combo); `Delay` switch + `Randomize` + `Value` 1-8 (1 = Off) + `Type` Default / Ways (`Slider` 2-6, `- 1..6`). Root `Enable Builder` + `Condition` combo. A disabled state yields nil (falls back to "Shared" which is not in the list) → nothing written that tick.
**Jitter engine**: flip only on un-choked commands; while `rage.exploit:get() == 1` the side is held for `delay` sent cmds; `Randomize Jitter` in Options → random side instead of alternating; the script strips `Jitter` / `Randomize Jitter` from the options before writing and drives `inverter` itself. `Center` modifier collapsed into `yaw_offset ± modifier_offset/2`, `Offset` one-sided, L/R offset by side. Spin lives only in the defensive module.
**Write map** (`var376`, all `:override`): enabled, pitch, yaw, base, offset, avoid backstab, hidden, yaw modifier + offset, body yaw / inverter / left / right / options / freestanding body, freestanding / disable yaw modifiers / body freestanding, extended angles / pitch / roll. Scratch table cleared each tick (`:override(nil)` = release).
**Pipeline** (createmove): clear → release HS / DT lag options → state → Force Defensive (+ tickbase) → AA On Use (Legit AA) else Builder → Avoid Backstab → Manual Yaw (highest) → Defensive → Freestanding → Edge Yaw → Safe Head → Flick Exploit → flip + collapse + write.
**Manual** Disabled / Left / Right / Forward / Backward (F 180, R 90, L -90, B 0; base Local View, freestanding false); `Body Freestanding` → body 60/60, options `{}`, freestanding body `Peek Fake`; `Disable Yaw Modifiers` → L/R 0, modifier Disabled, body true, inverter false, limits 0/0, freestanding body Off. **Freestanding** switch + Disable Yaw / Body Modifiers + Body Freestanding.
**Force Defensive**: switch + `Conditions` (7 states); `Tickbase` switch, `Randomize`, `Choke` 2-22 def 16, `Type` Default / Ways (`Sliders` 3-6, `- 1..6` 2-22 def 16). Forces `Hide Shots > Options "Break LC"` + `Double Tap > Lag Options "Always On"`, then `cmd.force_defensive = command_number % choke == 0` (or `% random(t1, t2)` / `% t[random]`); sets `hidden = false`.
**Defensive** (only while `rage.exploit:get() == 1`; detection `m_nTickBase` backwards → `defensive_ticks = clamp(prev - cur - 1, 0, 14)`, `max_defensive_ticks` latched on the first shift tick): per state `Enable`, `Pitch` Default / Up (-89) / Down (89) / Zero / Random / Progressive (`(curtime*7 % 2 - 1) * 89`) / Static Random (re-rolls From / To 50-50 at defensive start) / Custom (offset); `Yaw` Default / Jitter (`band(sent_packets,1) ~= 0 ? -90 : 90`) / Opposite (180) / Spin (`curtime * (offset*12) % 360`) / Random / Povorotniki (`tickcount % 3 == 1 ? (parity ? -90 : 90) : (parity ? 90 : -90)`) / Progressive Spin (`(curtime*7 % 3 - 1) * 179`) / Static Random / Custom (`-offset`). Applied via `rage.antiaim:override_hidden_pitch / override_hidden_yaw_offset`, `hidden = true`, HS "Break LC" + DT "Always On"; on `tickcount % 3 ~= 1` a full body-modifier zero-out (limits 0/0, inverter false, options `{}`, freestanding body Off) — real angle frozen 2/3 of ticks while the hidden angle moves.
**Safe Head**: switch + Conditions Knife / Zeus / Distance; requires airborne AND `duck_amount >= 0.66`; Knife = `weapon_type == 0` and index ≠ 31; Zeus = index 31; Distance = `length2dsqr > 1,000,000` and not knife / zeus. Output: pitch Down, yaw offset 0, base At Target, modifier Disabled, body true, inverter false, limits 0/0, options `{}`, freestanding body Off, hidden false.
**Flick Exploit** ("wateroff"): `Magic key` 1-25 step 0.1 s def 1; toggles every interval, alternating yaw ±90 at At Target, pitch Down, body 60/60, options `{}`, **`cmd.force_defensive = true`**; inactive phase yaw 0, modifier off, offset 20, body false, options `{"Jitter"}`, force_defensive false.
**Edge Yaw**: 20 rays at 18° steps, 32u, from the eye (`utils.trace_line(eye, eye + vector():angles(0, deg) * 32, lp, 0xFFFFFFFF, 1)`), needs ≥ 2 world hits; `mid = (first + last) / 2`, `delta = normalize_yaw(-view.y + mid)`, bail if `|delta| > 90`, else `yaw_offset += delta*2 + 180`, base Local View, hidden false.
**Anti-Aim On Use** + `Bombsite E Fix`: suppressed in a bomb zone (E fix: only with the C4, classid 34), as CT near a ticking `CPlantedC4` (`length2dsqr < 10000`), when a 128u hull trace hits classid 97 or a first-press eye trace hits a usable entity; when it fires: `cmd.in_use = 0`, yaw +180, pitch Disabled, base Local View (unless mode "Legit AA"), freestanding / extended / hidden false.
**Animations**: `Options` Landing Pitch / Force Falling / Move Lean / Leg Breaker, `Move Lean Force` 0-100, `On Ground Force`, `Moonwalk Mode`, `Moonwalk In Air Mode`; `events.post_update_clientside_animation`, layers at `animstate_ptr + 10640`: Force Falling → `pose[6] = 0.5` airborne; Leg Breaker → ground `pose[0] = 1` + Leg Movement "Sliding" (Moonwalk → `pose[7] = 0` + "Walking"; in air with Moonwalk In Air → `layer[6].weight = 1`, `cycle = curtime*0.55 % 1`); Move Lean → `layer[12].weight = clamp(w + w*(force*1000), 0, 1)`; Landing Pitch → `pose[12] = 0.5` on landing.

## Built-in preset "Author" (decoded)

Builder (all states: pitch Down, yaw Backward, body yaw true, limits 60/60, options `["Jitter"]`, freestanding Off):

| State | base | L | R | modifier | mod offset | delay |
|---|---|---|---|---|---|---|
| Standing | At Target | -35 | 30 | Disabled | 24 | 1 |
| Moving | At Target | -25 | 30 | Center | -24 | 0 |
| Slow Walk | **Local View** | 0 | 0 | Center | -8 | 2 |
| Air | At Target | -40 | 30 | Center | 13 | 0 |
| Air Crouch | At Target | 3 | 13 | Center | -51 | 0 |
| Crouch | At Target | -19 | 46 | Disabled | 46 | 2 |
| Crouch Move | At Target | 0 | 13 | Center | -46 | 4 |

Defensive (master off, condition Crouch): Standing off Zero 89 / Spin 25; Moving off; Slow Walk on Progressive / Povorotniki -24; Air off Progressive 25 / Povorotniki 73; Air Crouch on Progressive / Jitter 78; Crouch on Up -9 / Progressive Spin -3; Crouch Move on Progressive / Povorotniki 30. Other: force defensive on {Slow Walk, Air, Air Crouch, Crouch, Crouch Move}, safe head Knife, avoid backstab, AA on use + E fix, edge yaw off, freestanding off, manual Disabled + disable yaw modifiers, animations Move Lean 100 on ground force. Non-AA: fast ladder, viewmodel fov 680 x 25 y 0 z -15 opposite knife, aimbot logs Console / Notify / Screen, dormant aimbot damage 5 hc 68 autoscope, grenade throw fix, no fall damage, super toss, arrows Default (Manual FFFFFF, Desync 21A3DE), scope overlay offset 6 position 112, indicate state offset 24 Alternative, hit marker, watermark Bottom, removals Spaces / Build.

## Ragebot helpers

Aimbot Logs (Output Console / Notify / Screen, hit / miss colors; `"hit ${name}'s ${hitbox} for ${dmg} damage"` / `"missed ${x}'s ${y} due to ${reason}!"`; `aim_ack` + synthesized `dormant_hit / dormant_miss`), Dormant Aimbot (hitboxes, Accuracy 50-85 def 60, Min. Damage -1..130 (-1 Auto, > 100 "+N"), Auto Scope), AI Peek (Scanning Hitboxes listable, Simulation Time 25-35 def 28, Rate Limit 0-30 def 2, Hit Chance 0-100 def 35 (0 = Def.), Unsafety, Developer Mode, Force uncharge, Range 15-25 def 20 t, Retreat 15-30 def 25 u; overrides per weapon SSG-08 / Desert Eagle / Pistols: Head / Body Scale 100, Hit Chance, Unsafety → Body Aim / Safe Points "Default" + Ensure Hitbox Safety `{}`; Retreat Mode "On Shot"; `force_teleport()`; `VDebugOverlay004` vfunc 21 hull), Grenade Throw Fix (pin pulled on `weapon_type 9` → `Weapon Actions :override({})`), Super Toss (8-iteration Newton solve with `simulate_movement()`, `in_speed`, Air Strafe / Strafe Assist off), Fake Latency (Value 0-1000 def 1000; `Fake Latency :override(v)` + `sv_maxunlag 0.4`), No Fall Damage (8-ray probe radius 10, depths 15 / 75 → `in_duck`).

## Visuals

Watermark Style DISABLED (DEV) / Simple / Default, color 153,178,255 (Simple = draggable text at `0.495w, 0.985h` with a color wave; Default = top-right panel at `(w-10, 10)`, `render.blur` + rounded rect + outline, `logo(stars + spectral) · user · fps · ms · HH:MM`, font museo500 14.2). Keybinds (draggable, header `keyboard` icon + "hotkeys", min width 130, smoothy width, `ui.get_binds()`, per-bind alpha fade, strips `\aDEFAULT` / `\a{...}` / non-ASCII). Scope Overlay (color, Position 0-500 def 105, Offset 0-500 def 10, T Style hides the top bar; `Remove All` override, 4 gradient bars scaled by `screen.y * 0.000926`, fades with `m_bIsScoped`). Anti-Aim Arrows (palettes Manual BEEC28 / Desync 21A3DE; Default = Verdana-16 `<` `>` `^` ±40px; Alternative = `render.poly` triangles + 2px stems, base 17,17,17,128 lerped to Manual (manual L/R) / Desync (`abs_body_yaw < -10` / `> 10`), 8 smoothy channels). Indicate State (Default: Verdana-12 at center+40: `spectral` / `freestand` (177,151,255) / `fake yaw`; `dynamic` (+`+` when `get_target(true)`) or `default` when Local View; `dt` (+`(fakeduck)`) green if charged else red; `aa`; typewriter `string.sub(s, 1, #s*t)`; Alternative: `BUILD` sine-bobbing chars + wave-colored `spectral` + DT / HS with shadow), Lethal Indicator (`! LETHAL !` pulse `0.75 + 0.25*sin(realtime*6)`, offset -32, Min HP 92), Damage Indicator (draggable min-damage box near the crosshair, opaque only while the menu is open), Velocity Warning (draggable blurred panel at `0.5w, 0.3h`, `triangle-exclamation` icon + "velocity" + %, visible when < 1 or menu open), Hit Marker (`player_hurt`, 0.5s, 4 diagonals, fade in the last 0.25s), Scope Animations (First / Third Person, `override_view` fov smoothy 0.05; third person keyed on `Force Thirdperson`), Kibit Marker (`aim_ack` hits → world cross at `ack.aim` for 3s, Vertical 00FF00 / Horizontal 00FFFF), Aspect Ratio (0-250 step .01, 125 / 133 / 166 / 160 labels, 4 preset buttons, `r_aspectratio`), Contrast Boost (`Post Processing :override(true)` for `interval/100` s after `aim_ack`). Draggable window framework `var69.new(id, {pos, anchor})` persisted in `db["spectral-db"]`, blocks `in_attack` while dragging.

## Misc

Fast Ladder (`m_MoveType == 9`, pitch 89, alternating `in_moveleft/right` + `in_forward/back`, yaw +90 / +150 / +30 by sidemove sign), Spectral Clantag (`net_update_start`, 33 frames `"        "` → `"spectral"` → scroll out, index `floor((tickcount + to_ticks(latency)) / 17) % 33 + 1`, `common.set_clan_tag`, NL Clan Tag overridden false), Viewmodel (FOV 0-1000 step .1 def 680, X 25 / Y 0 / Z -15, opposite knife hand flips `cl_righthand` on `weaponselect` when `weapon_type == 0`; values ×0.1).

## FFI / vfuncs

`VDebugOverlay004` vfuncs 1 (AddBoxOverlay), 20 (AddLineOverlayAlpha), 21 (AddBoxOverlay2) — AI Peek hull for `tickinterval * 2` with the Peek Assist Style color. vgui2 `VGUI_System010` vfuncs 7 / 9 / 11 (`GetClipboardTextCount / SetClipboardText / GetClipboardText`) for config import / export. Anim layer struct at `animstate + 10640`. Config export = table of `{value}` (color pickers `{selected, {palette → hex[]}}`) → `json.stringify` → base64 → clipboard; in-built presets un-savable / un-deletable.

## Extra ui.find paths

`Visuals,World,Main,Force Thirdperson,Distance`; `Visuals,World,Other,Hit Marker,3D Marker / Damage Marker`; `Visuals,World,Other,Grenade Prediction,Color / Color Hit`; `Visuals,World,Ambient,Post Processing`; `Aimbot,Ragebot,Safety,<weapon>,Body Aim / Safe Points / Ensure Hitbox Safety`; `Aimbot,Ragebot,Selection,<weapon>,Min. Damage / Hit Chance / Multipoint,Head Scale / Body Scale` (weapon = SSG-08, Desert Eagle, Pistols).
