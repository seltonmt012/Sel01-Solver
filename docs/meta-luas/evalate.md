# evalateRecode.lua (8,959 lines, decompiled) — evalate 2 "Recode"

Libs: `neverlose/pui` (P), `base64`, `hashing`, `gradient`, `clipboard`, `websockets`. pui colors red 255,125,125 / grey 141,141,141 / green 169,182,81. Three sidebar tabs (`pui.sidebar("evalate 2", "signal-stream")`, `pui.setup({antiaim, features}, true)`): Home (`house-blank`: Overview / Presets), Features (`wave-sine`: Aimbot / Visual / Misc), Anti-Aim (`shield`: Setup / Builder / Defensive). Sub-page selectors are `:list("", {...})`; `menu.set_callback_list` rewrites the selected entry to `"\v•  \r%s"` for a highlighted bullet. Labels `"\v\f<icon>    \rName"`.

## Anti-aim

**States**: `Global, Stand, Run, Walk (slow), Crouch, Creeping (crouch-move), Air, Air crouch, On use`; defensive states replace On use with `On peek`; teams `Base / Terrorists / Counter-Terrorists` optional (`Apply to opposite team` button). Detection `(in_jump, on_ground, anim_duck_amount, velocity)`: On use → Stand (vel < 2, duck < 0.7) → Air crouch (duck > 0.8) → Air → Creeping (duck > 0.8, vel > 2) → Crouch → Run (vel > 2, not slow walk) → Walk (slow walk); first match wins; a state needs its `Enable` switch, fallback Global.

**Per state**: `Offset` -180..180 → `Add sides` (`Add left` / `Add right` -90..90) + `Add random` 0-50; `Modifier` None / Center / Offset / Random / Ways / Spin / Sway + `Min / Max` switch (else symmetric ±degree) + `Custom ways` (3-8 degree sliders) + `Apply delay` (true; syncs the modifier flip to the desync side instead of `tickcount % 4 >= 2`); `Ways` 3-8, `Speed` 1-100 def 50 (Spin = saw `lerp(min, max, realtime % (speed/100) / (speed/100))`, Sway = ping-pong `|t*2-1|`), `Degree`, `Min` / `Max`. **Desync**: switch; `Limits` Static / Random (L/R 0-58 def 58, min 29 / max 52); `State` Always on / Random (`random(0,2) == 0`) / Delay (1-20 def 6t); options `Jitter` switch, `Inverter` switch, `Freestanding` Off / Peek Real / Peek Fake. **Extra > Delay** (the switch-timing engine, real cmds only): `Mode` Static (2t) / Staged (2-8 stages, each 1-16 def 2) / By sides (Left 1 / Right 16, or with Min / Max: Left 1-4, Right 10-16) / Random (min 2, max 5). **Freeze** (anti-bruteforce): chance 0-100 def 50 %, duration 1-80 def 10t — while the freeze counter runs the inverter is NOT flipped. `Force disable` {Manual, Freestand}.
Yaw math: `base = offset (+ add_left/right by inverter) (+ random)`; `flip = tickcount % 4 >= 2` or the desync side; Center `flip ? min : max`; Offset `flip ? min : 0`; Random; Ways `linspace(n, min, max)[idx]` (idx advances per real cmd); on use `+180`; manual + static = `base + manual`, no modifier.
Writes each createmove: `Enabled true`, `Yaw "backward"`, `Yaw Modifier "disabled"`, `Base` at target (safe head) / local view (on use / manual) / UI `View` combo, `Offset` computed, `Pitch` down (disabled on use), `Body Yaw` true (false if desync off / unsafe yaw; random for State = Random; delay flag for State = Delay), limits static or `random(min, max)`, `Inverter` = script side (or UI switch), `Body Yaw > Freestanding` combo, `Options {}`, `Freestanding` (+ Disable Yaw Modifiers / Body Freestanding = static flag) when the Freestand bind is held, `Avoid Backstab`.
**Safe head**: `States` Air / Stand / Crouch, `Air options` Zeus / Knife / Other weapons too, `H. Gap` 0-100 def 35 (`me.z - gap > threat.z`); Air = gap + weapon + airborne; Stand = grounded, not ducking, vel < 2; Crouch = grounded + ducking. Output: base at target, body yaw true, **limits 5/5**, inverter true, offset 0.
**Unsafe yaw** (Warmup / No enemies): Yaw Spin (`spin(1, -180, 180)`) / Random, Pitch Zero / Down, desync off. **Manual** Backward / Forward / Right / Left + Static. **Freestand** switch + Static. **On use**: `allow_on_use` + not holding a drop-grenades bind + not blocked (holding C4; 200u trace hits `weapon|deagle|door|button`; CT within 125u of a non-rescued `CHostage` or any `CPlantedC4`) + `cmd.in_use` → swallow, base local view, pitch disabled, yaw +180. `Fake duck on warmup`, `Disable defensive`, `Fake flick` (disabled).

**Defensive** (detection `m_nTickBase` regression, `defensive_ticks_left = min(14, last - tb - 1)`): per def state `Enable` + `Remove modifier`; `Force Break LC` → `Allow double tap` (true) / `Allow hide shots` (true); `Custom tickbase` → `Static` 1-22 def 16 (`cmd.force_defensive = command_number % static == 0`); Yaw `None / Swap / Static / Switch / Random / Ways / Spin / Sway / Peek direction` (Ways 3-8, degrees, Speed 1-200 def 100 ×0.01s, Delay 1-20 (1 = Off)); Modifier same set; Pitch `None / Static / Swap / Switch / Random / Ways / Spin / Sway` (-89 "Up", 89 "Down", 0 "Zero"); `Custom duration` Static / Staged / Random, stages 1-13. Runtime: DT options `"always on"` / HS options `"break lc"` while active (not unsafe yaw / on use / safe head / manual), `Yaw > Hidden` = def state exists and not disabled, `override_hidden_pitch(pitch)`, `override_hidden_yaw_offset(-yaw_total)` gated by the duration; Swap flips on the defensive-window edge, Switch on real cmds, Peek direction uses `ctx.peek_yaw - manual_offset` (peek detection every 20 ticks: `simulate_movement():think(13)`, trace bullet from the predicted origin to threat hitbox 6). Known leak bug: L8813 tests a bare global.

## Ragebot helpers (Features > Aimbot)

Predict (`cl_interpolate / cl_interp_ratio / cl_interp`: Soft 1/3/0.031, Medium 1/2/0.026, Extreme 1/1/0.015625, Ultimate 0/1/0.015625; restored 1/2/0.015625), AI Peek (simulation 25-35 def 30 ms, process limit 0-30 def 3 ms, hitboxes, weapons snipers / pistols, show simulation; `VDebugOverlay004` boxes; bind to the Peek Assist key), Jump scout (ssg08 / revolver, no move input, vel ≤ 1.2, airborne → `Air Strafe :override(false)` + `Auto Stop options :override({"In Air"})`), Air exploit (Duration 5-40 def 10t: airborne non-grenade, `exploit_charge == 1 and tickcount % ticks == 0` → `cmd.force_defensive = true`, `double_tap.lag_limit:override(math.random(7))`, `rage.exploit:force_teleport()`; else `force_charge()`), Air teleport (Allow on cross, weapons Awp / Scout / Taser; airborne + DT + charge → `simulate_movement():think(6)`, trace bullet to hitbox 3, dmg > 10 → `force_teleport()`), Auto OS (states Stand / Crouch / Slow walk, avoid Pistols / Desert Eagle / Auto Snipers / Deagle & Crouch; DT on, HS off, charge 1, Peek Assist not active → `hide_shots:override(true)`, `double_tap:override(false)`), Dormant aimbot (Damage 0-100 def 20, Inaccuracy 0-100 def 80; network-state labels "100% info" / "updated by shared esp" / "updated by sounds" / ...).

## Visuals

Accent color Static / Rainbow; Settings Scaling / Blur (true) / Glow (true) — `render.blur` and `render.shadow` are monkey-patched globally; Notifications Debug / Screen / Console; Watermark (Version / Username source Cheat / Steam / Custom / Latency / Frames / Time 24h-12h, Position Top Right / Top Left / Bottom Center; animated `𝒆𝒗𝒂𝒍𝒂𝒕𝒆` gradient); Spectators; Hotkeys (`ui.get_binds()`, strip color escapes, hold / toggle); **Crosshair** (centered stack: animated header + desync bar = two mirrored `render.gradient` wings sized by `min(|normalize_yaw(get_rotation(true) - get_rotation())|, max_desync) / max_desync` drawn ≥ 0.9, then `DT` with `render.circle_outline(..., 0, exploit_charge)` ring, `HS`, `DMG`; dimmed to alpha 100 when the script overrides the flag off); Damage (font, Animate, Always on); Arrows Primed / Delicate (inline SVG via `render.load_image`, lights the side of the manual combo, inactive `color(0, 75*alpha)`); Scope (Gap 0-100 def 5, Length 0-300 def 55, Invert, custom color 255,121; overrides Scope Overlay); Slow down bar (`m_flVelocityModifier`, white → yellow → red at 0.7); Fake lag (Lines / Circle from a 14-sample `choked_commands` history, shows 2.5s after choke > 3); Exploit `EX` ring shrinking while defensive; Viewmodel (fov 0-1500 def 680, x 25, y 0, z -20, Opposite knife, Reset); Aspect ratio 100-250 def 140; **Anim. breaker** (Air None / Static / Walking, Ground None / Slide / Shake / Walking, Other Flashed / Move lean / Landing pitch / Static creeping, Lean weight 0-100): Slide → Leg Movement "Sliding" + `m_flPoseParameter[0] = 0`; Walking (ground) → "Walking" + `pose[7] = 0`; Shake → "Sliding" + `pose[0] = (tickcount % 4 > 1) and 0.5 or 0`; Static (air) → `pose[6] = 1`; Walking (air) → `layers[6].weight = 1; pose[7] = 0`; Flashed → `layers[0].sequence = 227`; Move lean → `layers[12].weight = lean * 0.01`; Landing pitch → `pose[12] = 0.5` on `anim_state.landing`; Static creeping → `pose[8] = 0`. Layer struct at `player + 10640`.
Widget framework: every HUD element registers a draggable anchor persisted as hidden sliders `drag[name].px / .py / .offset`; the stack renderer packs enabled indicators by accumulated alpha so they slide as items fade.

## Misc

Fast ladder (`m_MoveType == 9`, pitch 89 + strafe keys), No fall damage (`velocity.z <= -500` + downward traces at 15 / 50 → `in_duck`), Avoid collisions (9 traces 20°-180° at 70u, rewrite move when a wall is < 32), Console color (materials `vgui_white`, `vgui/hud/800corner1..4`; gated on `VEngineClient014[11]`), Flash game icon (user32 `FindWindowA("Valve001", ...)` + `FlashWindow` on round_start), Client-side nickname (opcode scan → userinfo `szName[128]` write; default "prince"), Super toss (`grenade_override_view`, pitch `-10 + |pitch|/9`, power `clamp(v*0.9, 15, 750) * (clamp(k,0,1)*0.7 + 0.3)`), Auto release (on pin pulled, min damage 1-60 def 20, Molotov / HE; `grenade_prediction` damage, toggles `in_attack` around `m_bPinPulled`), Drop grenades (staggered `use weapon_x; drop`), Clantag (29-frame `evalate.com` typewriter, step 17 ticks, latency-compensated `tickcount + to_ticks(latency)`), Voice state Unmute / Mute (`GameClientExports001` vfuncs 1/2/3), Shared icon (hashed steamid64 → GitHub PNG, `entity:set_icon` every 64 ticks), Trashtalk (On Kill / On Death, Simple / Russian / English), Unlock latency (`sv_maxunlag 1`), Unlock fake duck speed (hooks the NL Fake Duck callback, renormalizes move to 450 in `createmove_run`).

## Presets / config

No hardcoded presets. Whole config: `clipboard.set(("evalate>%s<"):format(base64(json(package:save()))))`; single state: `evalate[Builder:T:Stand]>b64<`; parse `json.parse(base64.decode(string.match(clipboard.get(), ">(.-)<")))`. `package:save/load(scope...)` = pui scoped save. Cloud presets via `api.evalate.com/v2` (Storage Cloud / Local, Sort Last Update / Most Liked / Most Loaded, Filter None / My / Liked). Managed event registry (`events[name]:set/unset` de-duplicated) so features attach / detach on toggle; global `ctx` refreshed on `pre_render` / `createmove` / `render` / `level_init`.

## ui.find refs (pui.find with sub-element alias tables)

`Aimbot,Anti Aim,Misc,Leg Movement`; `Aimbot,Ragebot,Accuracy,SSG-08,Auto Stop` {double_tab, options}; `Aimbot,Anti Aim,Angles,Freestanding` {yaw = "Disable Yaw Modifiers", body = "Body Freestanding"}; `Aimbot,Ragebot,Main,Double Tap` {lag_limit = "Fake Lag Limit", options = "Lag Options", quick_switch = "Quick-Switch"}; `Aimbot,Ragebot,Main,Hide Shots` {options}; `Miscellaneous,Main,Other,Weapon Actions`; `Miscellaneous,Main,Movement,Air Strafe / Strafe Assist`; `Aimbot,Ragebot,Safety,Body Aim / Safe Points`; `Aimbot,Ragebot,Main,Peek Assist` {retreat_mode}; `Visuals,World,Main,Override Zoom,Scope Overlay`; `Aimbot,Ragebot,Selection,Hit Chance / Min. Damage`; `Aimbot,Anti Aim,Misc,Slow Walk / Fake Duck`; `Aimbot,Anti Aim,Fake Lag,Enabled`; `Miscellaneous,Main,Other,Unlock Hidden Cvars`; all `Aimbot,Anti Aim,Angles,*`. `is_min_damage()` = `ui.get_binds()` entry with `reference:id() == damage:id()` and `.active`.
