# Sel01 scripts — latest published versions.
# Every script fetches this file once at load and compares it to its own constant.
# Format: key=version   (bump the line in the SAME commit as the version constant)
solver=11.31
config=4.1
walkbot=2.2
plantbot=1.10
